Aquí reside todas las rutas de los Websockets del sistema como del chat, comentarios, notificaciones, etc. que estas comunicaciones son en tiempo real.

Para mejorar la comunicación en tiempo real se está usando la libraría `meloy` de go
```go
func PublicWs(e *echo.Echo) {
	ws := e.Group("/ws")

	// Comment comment
	mel := melody.New()
	mel.Config.MaxMessageSize = 1024 * 1024 * 1024
	mel.Upgrader.CheckOrigin = func(r *http.Request) bool {
		return true
    }
    ...
}

```