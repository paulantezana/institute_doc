## Tabla Role
Se usa para almacenar los diferentes roles de un usuario
```go
type Role struct {
	ID   uint   `json:"id" gorm:"primary_key"`
	Name string `json:"name"`
}
```
## Campos
- **Name(requerido)** Campo para almacenar el nombre del rol